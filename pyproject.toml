[project]
name = "bank-migration"
version = "1.0.0"
description = "UN Wallet Multi-Bank Data Migration Platform - Production-Grade Interbank ETL"
requires-python = ">=3.11"
dependencies = [
    "celery>=5.6.3",
    "cryptography>=48.0.0",
    "defusedxml>=0.7.1",
    "fastapi>=0.136.1",
    "openai-agents>=0.17.2",
    "openpyxl>=3.1.5",
    "psycopg2-binary>=2.9.12",
    "pydantic>=2.13.4",
    "python-docx>=1.2.0",
    "python-multipart>=0.0.28",
    "redis>=7.4.0",
    "uvicorn>=0.46.0",
]

[project.scripts]
bank-migration = "api_only:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
